# Интеллектуальная самообучающаяся система распознавания лиц «Персона ИД»

## Описание

Интеллектуальная самообучающаяся система распознавания разработана на Python и имеет веб интерфейс на JavaScript React. Подсистема распознавания лиц основывается на библиотеках OpenCV, Dlib, Deepface и TensorBoard. Также были исследованы следующие библиотеки: VGG-Face, Google FaceNet, OpenFace, DeepИД, ArcFace. Данная система способна не только распознавать лица, у неё также имеется ряд механизмов для самообучения, что выгодно отличает данную систему от многочисленных имеющихся аналогов.

## Области применения

- Изучение современных моделей и методов искусственного интеллекта и машинного обучения на примере распознавания лиц.
- Исследование и визуализация моделей, данных и механизмов обучения глубоких нейронных сетей.
- Учет посещения занятий студентами на основе алгоритмов распознавания лиц в автоматическом режиме. Повышение мотивации к обучению за счет выявления проблем с посещаемостью у студентов на ранних стадиях.
- Нахождение на территории образовательного учреждения только авторизованных посетителей и моментально реагировать на появление нежелательных лиц как в здании, так и в периметре.
- Обучение и экзаменация. Решаются вопросы контроля за соблюдением установленных правил во время экзаменов и аттестаций.

Аналитическим ядром информационной системы является система видео аналитики, для реализации которой используется язык программирования Python с библиотеками обработки изображений. Задачи, связанные с распознаванием лиц включают в себя следующее:

- Обнаружение лица (определение местонахождение лица, включенного в изображение, и более детальное расположение ключевых точек, таких как глаза, нос, рот и т. д.).
- Отслеживание лица (отслеживание изменений положения лица в видео)
- Проверка лица (введите два лица, чтобы определить, принадлежат ли они одному человеку)
- Распознавание лиц (введите лицо, чтобы определить, какой человек принадлежит ко всем записям в базе данных лиц)

Разработка программного средства осуществлялась в рамках научно-практической деятельности кафедр КБ-2 и КБ-3, используется в качестве программного стенда при проведении практических занятий по дисциплинам, связанным с обработкой и анализом данных.

---

## 📋 Содержание

1. [Системные требования](#системные-требования)
2. [Установка и настройка](#установка-и-настройка)
3. [Структура проекта](#структура-проекта)
4. [Запуск системы](#запуск-системы)
5. [Конфигурация](#конфигурация)
6. [Устранение неполадок](#устранение-неполадок)

---

## Системные требования

### Обязательные компоненты:

- **Python**: 3.10, 3.11 или 3.12 (рекомендуется 3.12)
- **Docker**: Последняя версия (для PostgreSQL)
- **Docker Compose**: Версия 3.9 или выше
- **Git**: Для клонирования репозитория
- **Камера**: USB-камера или встроенная веб-камера

### Операционная система:

- Windows 10/11
- Linux (Ubuntu 20.04+, Debian 11+)
- macOS (с некоторыми ограничениями)

### Аппаратные требования:

- **CPU**: 4+ ядра (рекомендуется)
- **RAM**: Минимум 8 GB (рекомендуется 16 GB)
- **Место на диске**: Минимум 5 GB свободного пространства

---

## Установка и настройка

### Шаг 1: Клонирование репозитория

```bash
# Клонируйте репозиторий
git clone https://github.com/just-geniuss/PersonaID.git

# Перейдите в директорию проекта
cd PersonaID
```

### Шаг 2: Установка Docker и Docker Compose

#### Windows:

1. Скачайте и установите [Docker Desktop](https://www.docker.com/products/docker-desktop/)
2. Запустите Docker Desktop
3. Убедитесь, что Docker работает:
   ```cmd
   docker --version
   docker-compose --version
   ```

#### Linux (Ubuntu/Debian):

```bash
# Установка Docker
sudo apt-get update
sudo apt-get install -y docker.io docker-compose

# Добавьте пользователя в группу docker
sudo usermod -aG docker $USER

# Перезайдите в систему для применения изменений
# или выполните: newgrp docker
```

### Шаг 3: Создание виртуального окружения Python

#### Windows:

```cmd
# Создайте виртуальное окружение
python -m venv venv

# Активируйте виртуальное окружение
venv\Scripts\activate
```

#### Linux/macOS:

```bash
# Создайте виртуальное окружение
python3 -m venv venv

# Активируйте виртуальное окружение
source venv/bin/activate
```

### Шаг 4: Установка Python зависимостей

```bash
# Обновите pip до последней версии
pip install --upgrade pip

# Установите зависимости из requirements.txt
pip install -r requirements.txt
```

**Примечание**: Установка может занять 10-15 минут, особенно для dlib и других больших пакетов.

### Шаг 5: Запуск PostgreSQL через Docker

```bash
# Перейдите в директорию с конфигурацией Docker
cd PostgresDocker

# Запустите PostgreSQL и pgAdmin
docker-compose -f stack.yml up -d

# Проверьте, что контейнеры запущены
docker ps
```

Вы должны увидеть два работающих контейнера:
- `postgres_container` (порт 5432)
- `pgadmin_container` (порт 5050)

### Шаг 6: Инициализация базы данных

```bash
# Вернитесь в корневую директорию проекта
cd ..

# Подождите 10-15 секунд, пока PostgreSQL полностью запустится

# Создайте таблицы в базе данных
docker exec -i postgres_container psql -U personauser -d personadb < PostgresDocker/DokerCommand.txt
```

**Параметры подключения к базе данных:**
- **Хост**: 127.0.0.1
- **Порт**: 5432
- **База данных**: personadb
- **Пользователь**: personauser
- **Пароль**: pgpwd4persona

### Шаг 7: Создание необходимых директорий

```bash
# Создайте директории для фотографий и кадров
mkdir -p photo new del capture
```

**Описание директорий:**
- `photo/` - Хранение фотографий для распознавания лиц
- `new/` - Новые фотографии для обучения системы
- `del/` - Удаленные фотографии
- `capture/` - Захваченные кадры с камеры

### Шаг 8: Добавление фотографий для распознавания

```bash
# Поместите фотографии людей в директорию photo/
# Формат имени: Имя_Фамилия.jpg или Name_Surname.jpg
# Пример: Ivan_Ivanov.jpg, Maria_Petrova.jpg

# Каждая фотография должна содержать четкое изображение лица
# Рекомендуемый формат: JPG, PNG
# Рекомендуемое разрешение: 640x480 или выше
```

### Шаг 9: Настройка камеры (опционально)

Отредактируйте файл `camera_config.py` для настройки параметров камеры:

```python
# Индекс камеры (0 = первая камера, 1 = вторая и т.д.)
CAMERA_INDEX = 0

# Предпочитаемое разрешение
CAMERA_WIDTH = 10000
CAMERA_HEIGHT = 10000

# Подробное логирование
VERBOSE_LOGGING = True
```

### Шаг 10: Проверка камеры

```bash
# Протестируйте камеру перед запуском системы
python util/test_cam.py
```

Если камера работает корректно, вы увидите:
- Сообщения о попытках подключения к камере
- Видео с камеры в отдельном окне
- Нажмите 'q' для выхода

---

## Структура проекта

```
PersonaID/
├── App.py                    # Flask веб-сервер для API
├── bot.py                    # Telegram бот (опционально)
├── bot_config.py             # Конфигурация бота
├── camera_config.py          # Конфигурация камеры
├── capture_streamer.py       # Захват видео с камеры
├── process.py                # Обработка и распознавание лиц
├── start_capture.py          # Запуск модуля захвата
├── start_proc.py             # Запуск модуля обработки
├── zdata.py                  # Работа с данными и эмбеддингами
├── cleanerDB.py              # Очистка базы данных
├── requirements.txt          # Python зависимости
├── camera_config.py          # Конфигурация камеры
│
├── PostgresDocker/           # Конфигурация PostgreSQL
│   ├── stack.yml            # Docker Compose конфигурация
│   ├── DokerCommand.txt     # SQL команды для создания таблиц
│   └── tables_sql.sql       # Схема базы данных
│
├── photo/                    # Фотографии для распознавания
├── new/                      # Новые фотографии для обучения
├── del/                      # Удаленные фотографии
├── capture/                  # Захваченные кадры
│
├── util/                     # Утилиты
│   └── test_cam.py          # Тест камеры
│
├── UPGRADE_GUIDE.md          # Руководство по обновлению (RU)
├── UPGRADE_GUIDE_EN.md       # Upgrade guide (EN)
└── CHANGES_SUMMARY.md        # Сводка изменений
```

---

## Запуск системы

### Полный запуск всей системы

Для работы системы распознавания лиц необходимо запустить **три компонента** в отдельных терминалах:

#### Терминал 1: Запуск модуля захвата видео

```bash
# Активируйте виртуальное окружение (если не активно)
source venv/bin/activate  # Linux/macOS
# или
venv\Scripts\activate      # Windows

# Запустите модуль захвата
python start_capture.py
```

**Что делает этот модуль:**
- Захватывает видео с камеры
- Обнаруживает лица на кадрах
- Сохраняет кадры в базу данных для обработки
- Создает виртуальную камеру (pyvirtualcam)

#### Терминал 2: Запуск модуля обработки и распознавания

```bash
# Активируйте виртуальное окружение (если не активно)
source venv/bin/activate  # Linux/macOS
# или
venv\Scripts\activate      # Windows

# Запустите модуль обработки
python start_proc.py
```

**Что делает этот модуль:**
- Получает кадры из базы данных
- Распознает лица с использованием face_recognition
- Сравнивает с эмбеддингами из базы
- Сохраняет результаты в таблицу zdash

#### Терминал 3: Запуск веб-сервера API

```bash
# Активируйте виртуальное окружение (если не активно)
source venv/bin/activate  # Linux/macOS
# или
venv\Scripts\activate      # Windows

# Запустите Flask веб-сервер
python App.py
```

**Что делает этот модуль:**
- Предоставляет REST API для веб-интерфейса
- Отдает результаты распознавания
- Обслуживает фотографии и захваченные кадры
- Доступен по адресу: http://localhost:5000

### Проверка работы системы

После запуска всех трех компонентов:

1. **Проверьте API:**
   ```bash
   curl http://localhost:5000/
   ```
   Должен вернуть: `FaceRecog Items: N` (где N - количество распознанных лиц)

2. **Проверьте pgAdmin:**
   - Откройте браузер: http://localhost:5050
   - Email: rusal@bk.ru
   - Пароль: pgadminpwd4persona
   - Подключитесь к базе данных personadb

3. **Проверьте виртуальную камеру:**
   - Откройте приложение видеоконференций (Zoom, Teams, Skype)
   - Выберите "OBS Virtual Camera" в настройках видео
   - Вы должны увидеть видео с распознаванием лиц

### Остановка системы

```bash
# В каждом терминале нажмите Ctrl+C для остановки процесса

# Остановите Docker контейнеры
cd PostgresDocker
docker-compose -f stack.yml down
```

---

## Конфигурация

### Настройка камеры

Отредактируйте `camera_config.py`:

```python
# Выбор камеры
CAMERA_INDEX = 0        # 0 - первая камера, 1 - вторая и т.д.

# Разрешение камеры
CAMERA_WIDTH = 10000    # Макс. ширина (будет ограничено камерой)
CAMERA_HEIGHT = 10000   # Макс. высота (будет ограничено камерой)

# Логирование
VERBOSE_LOGGING = True  # True - подробные логи, False - минимум
```

### Настройка базы данных

Если нужно изменить параметры подключения к PostgreSQL, отредактируйте:

**В файлах Python** (App.py, capture_streamer.py, process.py, zdata.py):
```python
connection = psycopg2.connect(
    user="personauser", 
    password="pgpwd4persona",
    host="127.0.0.1", 
    port="5432",
    database="personadb"
)
```

**В Docker Compose** (`PostgresDocker/stack.yml`):
```yaml
environment:
  POSTGRES_DB: "personadb"
  POSTGRES_USER: "personauser"
  POSTGRES_PASSWORD: "pgpwd4persona"
```

### Параметры распознавания

В файле `process.py`:

```python
max_face_distance = 0.5  # Порог распознавания (0.0-1.0)
                         # Меньше = строже, больше = свободнее
                         # Рекомендуется: 0.4-0.6
```

В файле `capture_streamer.py`:

```python
detection_score = 0.4       # Порог обнаружения лица (0.0-1.0)
minDetectionCon = 0.6       # Минимальная уверенность детекции
number_of_processing_frame = 7  # Обрабатывать каждый N-й кадр
```

---

## Устранение неполадок

### Проблема: Камера не обнаружена

**Решение:**

1. Проверьте подключение камеры:
   ```bash
   python util/test_cam.py
   ```

2. Попробуйте другой индекс камеры в `camera_config.py`:
   ```python
   CAMERA_INDEX = 1  # или 2, 3...
   ```

3. Закройте другие приложения, использующие камеру (Zoom, Skype и т.д.)

4. На Linux добавьте пользователя в группу video:
   ```bash
   sudo usermod -a -G video $USER
   # Перезайдите в систему
   ```

### Проблема: Ошибка подключения к базе данных

**Решение:**

1. Проверьте, что Docker контейнеры запущены:
   ```bash
   docker ps
   ```

2. Перезапустите PostgreSQL:
   ```bash
   cd PostgresDocker
   docker-compose -f stack.yml restart
   ```

3. Проверьте логи контейнера:
   ```bash
   docker logs postgres_container
   ```

### Проблема: Ошибки при установке зависимостей

**Решение для dlib на Windows:**

```cmd
# Установите Visual Studio Build Tools
# Скачайте с: https://visualstudio.microsoft.com/downloads/

# Или используйте предсобранный wheel:
pip install dlib==19.24.6
```

**Решение для Linux:**

```bash
# Установите необходимые системные пакеты
sudo apt-get install -y build-essential cmake
sudo apt-get install -y libopencv-dev python3-opencv
```

### Проблема: ModuleNotFoundError

**Решение:**

```bash
# Убедитесь, что виртуальное окружение активировано
source venv/bin/activate  # Linux/macOS
# или
venv\Scripts\activate      # Windows

# Переустановите зависимости
pip install -r requirements.txt
```

### Проблема: Лица не распознаются

**Решение:**

1. Проверьте качество фотографий в папке `photo/`:
   - Четкое изображение лица
   - Хорошее освещение
   - Лицо занимает большую часть кадра

2. Убедитесь, что система обучена:
   - Поместите фотографии в папку `new/`
   - Система автоматически обработает их через 75 кадров

3. Настройте порог распознавания в `process.py`:
   ```python
   max_face_distance = 0.6  # Увеличьте для более свободного распознавания
   ```

---

## Web клиент на JS React

Web клиент на JavaScript React находится в файле `Sourcecode Front web React Client.zip`

Для запуска веб-интерфейса:

1. Распакуйте архив `Sourcecode Front web React Client.zip`
2. Следуйте инструкциям в README внутри архива
3. Убедитесь, что Flask API (App.py) запущен и доступен

---

## Дополнительная информация

### Документация:

- **UPGRADE_GUIDE.md** - Подробное руководство по обновлению системы
- **UPGRADE_GUIDE_EN.md** - Upgrade guide in English
- **CHANGES_SUMMARY.md** - Полная сводка изменений

### Поддержка:

При возникновении проблем:

1. Проверьте логи в консоли каждого модуля
2. Изучите документацию в папке проекта
3. Убедитесь, что все зависимости установлены корректно
4. Проверьте, что Docker контейнеры работают

### Технические характеристики:

- **Библиотеки распознавания**: face_recognition, dlib, OpenCV, mediapipe
- **Веб-фреймворк**: Flask 3.1.0 + Flask-CORS 5.0.0
- **База данных**: PostgreSQL 14.4
- **Виртуальная камера**: pyvirtualcam 0.12.0
- **Python**: 3.10+ (протестировано с 3.12.3)

---

## Лицензия

См. файл LICENSE в корне проекта.

---

## Авторы

Разработка программного средства осуществлялась в рамках научно-практической деятельности кафедр КБ-2 и КБ-3.

---

**Последнее обновление:** 2024-12-19
